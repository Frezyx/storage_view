name: flutter_secure_storage_view_driver
description: Flutter secure storage driver for storage_view. Flutter inspector tool for any database, storage
version: 0.1.0-dev.5
homepage: https://github.com/Frezyx/storage_view

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  storage_view: ^0.1.0-dev.5
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mocktail: ^0.3.0
  plugin_platform_interface: ^2.1.8
  flutter_secure_storage_platform_interface: ^2.0.1

flutter:
